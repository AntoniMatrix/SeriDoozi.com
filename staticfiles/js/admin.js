/* document.addEventListener('DOMContentLoaded', function() {
  const menuButtons = document.querySelectorAll('.card .stat.btn');
  
  menuButtons.forEach(button => {
    button.addEventListener('click', function(e) {
      // حذف کلاس secondary از همه دکمه‌ها
      menuButtons.forEach(btn => btn.classList.add('secondary'));
      
      // حذف secondary از دکمه کلیک شده
      this.classList.remove('secondary');
    });
  });
});
*/